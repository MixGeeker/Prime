# VITE_BACKEND_BASE_URL
# 含义：Compute Engine 后端地址（Studio / Ops 都通过它访问 Admin API 与 Node Catalog）
# 默认：http://localhost:4010
VITE_BACKEND_BASE_URL=http://localhost:4010

# VITE_ADMIN_TOKEN
# 含义：危险运维端点（如 DLQ replay）使用的 Bearer Token；可留空
VITE_ADMIN_TOKEN=
